# Banco de dados — Neon (https://neon.tech)
# DATABASE_URL usa connection pooling (PgBouncer)
# DIRECT_URL é a conexão direta (necessária para migrations)
DATABASE_URL="postgresql://user:password@ep-xxx.neon.tech/neondb?sslmode=require&pgbouncer=true"
DIRECT_URL="postgresql://user:password@ep-xxx.neon.tech/neondb?sslmode=require"

# JWT — gere com: openssl rand -base64 32
JWT_SECRET="troque-por-um-segredo-forte"

# URL pública da API (sem barra no final)
NEXT_PUBLIC_API_URL="https://seu-projeto.vercel.app"

# Cron secret — protege o endpoint /api/cron/capi
CRON_SECRET="troque-por-um-segredo-forte"

# Opcional: código de teste do Meta Events Manager
# META_TEST_EVENT_CODE="TEST12345"

# WhatsApp Cloud API (Meta) — token de verificação do webhook CTWA
WHATSAPP_WEBHOOK_TOKEN="troque-por-um-segredo-forte"

# UazAPI — configurado por workspace via painel (não precisa de env global)
# As credenciais uazapiUrl e uazapiToken ficam salvas por workspace no banco

# OpenAI — usado pelo Content Studio (geração de texto e imagem dos carrosséis)
OPENAI_API_KEY="sk-..."

# Meta Ads (Marketing API / Insights) — token de agência com permissão ads_read
# (diferente do metaAccessToken por workspace, que é da Meta CAPI). Usado por
# lib/meta-ads.ts para sincronizar gasto/impressões/cliques via /api/cron/ads-sync
# e pelo botão "Atualizar agora" em Tráfego Pago.
META_ADS_ACCESS_TOKEN=""

# Google Ads API — a agência tem 2 MCCs (Brasil e EUA), cada uma com seu próprio
# developer token, projeto do Google Cloud (Client ID/Secret) e refresh token.
# Client ID/Secret são por MCC, não compartilhados: um projeto do Google Cloud
# fica permanentemente vinculado ao primeiro developer token usado nele — usar o
# mesmo Client ID pras duas MCCs trava a segunda com DEVELOPER_TOKEN_PROHIBITED.
# Sem essas env vars, isGoogleAdsConfigured() retorna false e o cron simplesmente pula.
GOOGLE_ADS_DEV_TOKEN_BR=""
GOOGLE_ADS_DEV_TOKEN_US=""
GOOGLE_ADS_LOGIN_CUSTOMER_ID_BR=""
GOOGLE_ADS_LOGIN_CUSTOMER_ID_US=""
GOOGLE_ADS_CLIENT_ID_BR=""
GOOGLE_ADS_CLIENT_SECRET_BR=""
GOOGLE_ADS_CLIENT_ID_US=""
GOOGLE_ADS_CLIENT_SECRET_US=""
GOOGLE_ADS_REFRESH_TOKEN_BR=""
GOOGLE_ADS_REFRESH_TOKEN_US=""
